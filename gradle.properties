org.gradle.jvmargs=-Xmx6G
org.gradle.daemon=false
# JDK 25 is required to run Gradle itself (MC 26.2 needs Java 25).
# Point Gradle at one via ~/.gradle/gradle.properties:
# org.gradle.java.home=<path-to-jdk25>
